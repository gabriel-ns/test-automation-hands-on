describe('KitchenAid Homepage', () => {
  const url = '/'

  beforeEach(() => {
    cy.viewport('iphone-x')
  })

  test('deve mostrar as os produtos mais procurados ao clicar na barra de busca', () => {
  })

  test('deve mostrar a funcionalidade de stories', () => {
  })

  test('deve esconder o header ao descer pela página, e mostrar novamente ao subir', () => {
  })

  test('deve mostrar o menu ao clicar no botão de menu hamburger', () => {
  })
})